# StackBlitz demos

Standalone demo projects used by StackBlitz. These mirror the repo examples but replace workspace deps with published package versions so GitHub tree imports run inside WebContainers.
